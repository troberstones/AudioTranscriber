#!/bin/bash
# Wrapper so `transcribeFiles` works from anywhere without activating the venv manually.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/venv/bin/python" "$SCRIPT_DIR/transcribe_files.py" "$@"
